name: Release pipeline (reusable)

# Build + smoke-test + attest + publish. Invoked from release-tag.yml and
# release-manual.yml; not directly triggerable. Callers compute version/tag/draft
# and pass them in.
#
# The deb ships with postinst seeding pkg0 into its own state with attest:"required"
# (PLAN.md §7.5), so an unattested release would brick every selfupdate. This
# pipeline therefore treats attestation as load-bearing: the publish job attests,
# then VERIFIES the attestation is live and valid via `gh attestation verify`
# before the release is created. If that check fails, no release goes out.

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
        description: 'Debian-shaped version (no v prefix, e.g. 0.1.0)'
      tag:
        required: true
        type: string
        description: 'Full tag name including v prefix (e.g. v0.1.0)'
      draft:
        required: true
        type: boolean
        description: 'Create the GitHub release as a draft (not published)'

permissions:
  contents: write       # create release + upload assets
  id-token: write       # sigstore OIDC for attestation
  attestations: write   # write build provenance attestation

jobs:
  build:
    name: Build & smoke-test deb
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Lint
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq shellcheck
          bash -n pkg0 scripts/install.sh
          shellcheck -x pkg0 packaging/build-deb.sh packaging/postinst scripts/install.sh

      - name: Build deb
        run: |
          set -euo pipefail
          sh packaging/build-deb.sh "${{ inputs.version }}" "$GITHUB_REPOSITORY"
          mkdir -p dist
          # Version-LESS canonical asset name for a stable releases/latest/download
          # URL (the version still lives in the control file and `pkg0 version`).
          # The name deliberately keeps the pkg0_*_all.deb shape: that's the glob
          # postinst has seeded into every install's state since v0.0.2, so existing
          # installs selfupdate right across the rename. Must stay in sync with
          # SELF_GLOB in packaging/postinst and the asset name in scripts/install.sh
          # — the smoke test below asserts the former.
          mv "pkg0_${{ inputs.version }}_all.deb" dist/pkg0_latest_all.deb
          # The one-line `curl ... | bash` installer, shipped at the equally stable
          # releases/latest/download/install.sh URL.
          cp scripts/install.sh dist/install.sh
          dpkg-deb -I dist/pkg0_latest_all.deb

      - name: Smoke test
        run: |
          set -euxo pipefail
          sudo apt-get install -y ./dist/pkg0_latest_all.deb

          # version stamped into the script
          pkg0 version | grep -F "${{ inputs.version }}"

          # postinst seeded pkg0 into its own state, attest: required, repo = this repo
          jq -e --arg r "$GITHUB_REPOSITORY" \
            '.[] | select(.pkg_name == "pkg0" and .repo == $r and .attest == "required")' \
            /var/lib/pkg0/state.json

          # the seeded glob must match the asset name this release actually ships —
          # a rename that breaks this would leave every install HELD on selfupdate
          glob=$(jq -r '.[] | select(.pkg_name == "pkg0").glob' /var/lib/pkg0/state.json)
          # $glob is unquoted on purpose: fnmatch semantics, same engine pkg0 uses
          case "pkg0_latest_all.deb" in
            $glob) ;;
            *) echo "::error::seeded glob '$glob' does not match shipped asset 'pkg0_latest_all.deb'"; exit 1 ;;
          esac

          # state is root-owned 0644 (unprivileged list must read it)
          [ "$(stat -c '%a %U' /var/lib/pkg0/state.json)" = "644 root" ]
          pkg0 list | grep -q '^pkg0 '

          # usage errors exit 2
          rc=0; sudo pkg0 install not-a-repospec </dev/null || rc=$?
          [ "$rc" -eq 2 ]

          # non-interactive `pkg0 remove pkg0` must refuse
          if sudo pkg0 remove pkg0 </dev/null; then
            echo "::error::pkg0 remove pkg0 should have refused non-interactively"
            exit 1
          fi
          command -v pkg0 >/dev/null

          # reinstall must not clobber the seeded state entry
          ts1=$(jq -r '.[] | select(.pkg_name == "pkg0").installed_at' /var/lib/pkg0/state.json)
          sudo apt-get install -y --reinstall ./dist/pkg0_latest_all.deb
          ts2=$(jq -r '.[] | select(.pkg_name == "pkg0").installed_at' /var/lib/pkg0/state.json)
          [ "$ts1" = "$ts2" ]

          sudo apt-get remove -y pkg0

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-assets
          path: dist/*
          if-no-files-found: error
          retention-days: 1

  publish:
    name: Attest & create release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v3
        with:
          subject-path: 'dist/*'

      - name: Verify attestation is live
        # pkg0 clients (and pkg0's own selfupdate) depend on this attestation being
        # fetchable and valid — prove it against the real API before releasing.
        # install.sh rides along: everything in the release is attested and checked.
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          for f in dist/*; do
            gh attestation verify "$f" --repo "$GITHUB_REPOSITORY"
          done

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: ${{ inputs.tag }}
          draft: ${{ inputs.draft }}
          prerelease: false
          generate_release_notes: true
          files: dist/*
          fail_on_unmatched_files: true
